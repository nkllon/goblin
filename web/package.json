{
  "name": "goblin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --sourcemap --outfile=../docs/app.js --format=esm && cp public/index.html ../docs/index.html && cp public/style.css ../docs/style.css"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
